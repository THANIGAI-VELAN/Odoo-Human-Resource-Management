{
  "name": "pro-hrms-backend",
  "version": "1.0.0",
  "description": "HRMS Backend API Server",
  "scripts": {
    "dev": "python -m uvicorn main:app --reload --port 8000",
    "start": "python -m uvicorn main:app --port 8000"
  }
}
